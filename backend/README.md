# Backend - Smart Campus Prototype

## Setup
- Node.js v16+
- From this folder run `npm install` to install dependencies (express, ws, cors, uuid).

## Run
1. Start backend: `node server.js`
2. In a separate terminal start simulator: `node data_generator.js`
3. Open frontend/index.html to view the dashboard (or serve frontend folder)
